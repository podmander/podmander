# Podmander service configuration example
# Deploy with: podmander-controller --test-config web-service.toml

[service.web]
image = "nginx:latest"
env = { FOO = "bar", BAZ = "qux" }
ports = ["80:80", "443:443"]
volumes = ["web-data:/data", "web-config:/etc/nginx"]
